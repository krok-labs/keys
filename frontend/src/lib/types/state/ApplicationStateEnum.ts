// keys-related
export enum KeysApplicationState {
    IDLE = 'idle',

    // Stage 1: User scans his card and picks keys he want
    PICKING = 'picking',

    // Stage 2: Committing transition
    COMMITTING = 'committing',
    COMMITTED = 'committed',
};

// cards-related
export enum CardsApplicationState {

};

